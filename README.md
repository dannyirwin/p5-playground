# p5-playground

Vite + p5.js creative-coding playground.

## Commands

```bash
npm install
npm run dev      # http://localhost:5173
npm run build
npm run preview
```

## Cursor Cloud

Repo-scoped agent config lives in `.cursor/`, `.agents/`, and `AGENTS.md`.
Cloud VMs restore skills via `.cursor/environment.json` `install`.
